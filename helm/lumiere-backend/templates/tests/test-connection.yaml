apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lumiere-backend.fullname" . }}-test-connection"
  labels:
    {{- include "lumiere-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lumiere-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
