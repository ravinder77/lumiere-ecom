apiVersion: v1
kind: Service
metadata:
  name: {{ include "lumiere-frontend.fullname" . }}
  labels:
    {{- include "lumiere-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lumiere-frontend.selectorLabels" . | nindent 4 }}
